import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Registration",
};

const RegistrationInfoPage = () => {
  return <div>RegistrationInfoPage</div>;
};

export default RegistrationInfoPage;
